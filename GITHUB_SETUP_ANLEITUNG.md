"\"# GitHub Setup Anleitung für Spikeball ELO\n\n🎅 **AKTUELLER STATUS (angezeigt durch vorherige Tools):\n- ✅ Git Repository ist initialisiert (existiert)\n- ✅ PREVIOUSLY: No commits yet (Muss bestätigt werden)\n- ✅ Einige Dateien wurden gestaged (muss bestätigt werden, ob der Commit funktioniert hat)\n\n---\n\n## 📋 SCHRITT-FÜR-SCHRITT ANLEITUNG\n\n### SCHRITT 1: Git-Status prüfen\n\nFühre diesen Befehl im Spikeball-Verzeichnis aus:\n```bash\ncd /home/server2/spikeball   # falls nicht bereits drin\ngit status\n```\n\n**Erwartetes Ergebnis (BEFORE commit):**\n```\nOn branch master\nNo commits yet\n...\nuntracked files:\n  ...\nnothing added to commit...\n```\n\n**Erwartetes Ergebnis (AFTER commit):**\n```\nOn branch master\nnothing to commit, working tree clean\n```\n\n---\n\n### SCHRITT 2: Commit prüfen/erstellen (falls noch nicht geschehen)\n\nFühre diesen Befehl aus, um den Commit zu erstellen:\n```bash\ncd /home/server2/spikeball\ngit add -A && git commit -m \"Initial commit: Spikeball ELO System mit Friends Feature\"\n```\n\nDann prüfe, ob der Commit existiert:\n```bash\ngit log --oneline\n```\n\n**Erwartetes Ergebnis:**\n```\nabcdef1 Initial commit: Spikeball ELO System mit Friends Feature\n```\n\n**WICHTIG:** Falls \"On branch master nothing to commit\" angezeigt wird und du KEINEN Commit siehst, dann war der Commit VERSUCH des AGENTEN NICHT ERFOLGREICH. Wiederhole Schritt 2.\n\n---\n\n### SCHRITT 3: GitHub Repository erstellen\n\n1. Öffne: https://github.com/new\n2. Repository name: `spikeball-elo`\n3. Wähle \"Public\" oder \"Private\" (wie du magst)\n4. **SEHR WICHTIG:** Mache KEINAlle Checkboxen (wie \"Initialize with README\")!\n5. Klicke \"Create repository\"\n6. Kopiere die Repository URL, die angezeigt wird:\n   ```\n   https://github.com/DEIN_BENUTZERNAME/spikeball-elo.git\n   ```\n   (Ersetze DEIN_BENUTZERNAME mit deinem GitHub Benutzernamen)\n\n---\n\n### SCHRITT 4: Remote hinzufügen\n\nIm Spikeball-Verzeichnis:\n```bash\ngit remote add origin https://github.com/DEIN_BENUTZERNAME/spikeball-elo.git\n```\n\nErsetze DEIN_BENUTZERNAME mit deinem GitHub Benutzernamen.\n\nPrüfen, ob Remote korrekt ist:\n```bash\ngit remote -v\n```\n\n**Erwartetes Ergebnis:**\n```\norigin  https://github.com/DEIN_BENUTZERNAME/spikeball-elo.git (fetch)\norigin  https://github.com/DEIN_BENUTZERNAME/spikeball-elo.git (push)\n```\n\n---\n\n### SCHRITT 5: Code zu GitHub pushen\n\n```bash\ngit branch -M main\ngit push -u origin main\n```\n\n**Falls du 2FA verwendest:**\n- Du musst ein Git Credential Helper installieren und Personal Access Token (PAT) verwenden\n- Für Anleitung: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token\n\n**Falls du Passwort vergessen hast:**\n- Setze es neu:\n```bash\ngit credential-manager-core version  # falls installiert\n# Oder verwende: https://github.com/settings/tokens\n```\n\n**Falls Push fehlschlägt mit \"error: remote unpacking failed\":**\n- Manchmal passiert das wenn GitHub Repo bereits Dateien hat\n- Versuche: `git pull origin main --allow-unrelated-histories --rebase`\n- Oder lösche das GitHub Repo und erstell es neu ohne README.md\n\n---\n\n### SCHRITT 6: Erfolg prüfen\n\nGehe zu: https://github.com/DEIN_BENUTZERNAME/spikeball-elo\n\n**Erwartetes Ergebnis:**\n- Alle Dateien werden angezeigt (src/, prisma/, package.json, etc.)\n- Du kannst Änderungen im Webinterface sehen\n\n---\n\n## \u001b[31;1mWICHTIG: DATENBANK BACKUP\001b[0m\n\nErstelle ein Backup von der Datenbank:\n```bash\ncp db/custom.db db/custom_backup_$(date +%Y%m%d_%H%M%S).db\nls -la db/*.db   # prüfe ob das Backup existiert\n```\n\nVerschiebe Backups in ein eigenes Verzeichnis:\n```bash\nmkdir -p backups\nmv db/custom_backup_*.db backups/\nls -la backups/\n```\n\n---\n\n## \u001b[33;1mFORAGE BEI PROBLEMEN\001b[0m\n\n### Problem: \"git: command not found\"\n\nLösung: Git installieren\n```bash\n# Linux (Ubuntu/Debian)\nsudo apt update && sudo apt install -y git\n\n# Linux (Fedora/RHEL)\nsudo dnf install git\n\n# macOS (sollte installiert sein)\nbrew install git\n\n# Windows: Download von https://git-scm.com/\n```\n\n### Problem: \"Permission denied (publickey)\"\n\nLösung: SSH Key zu GitHub hinzufügen oder PAT verwenden\n```bash\n# SSH Key generieren (falls noch nicht vorhanden)\nssh-keygen -t ed25519 -C \"deine_email@example.com\"\ncat ~/.ssh/id_ed25519.pub   # Kopiere diesen Key und füge zu GitHub hinzu\n# In: https://github.com/settings/keys\n```\n\n### Problem: \"refusing to merge unrelated histories\"\n\nLösung: GitHub Repo hat schon Files (README.md etc.)\n```\ngit remote add origin https://github.com/DEIN_BENUTZERNAME/spikeball-elo.git\ngit pull origin main --allow-unrelated-histories --rebase\ngit push -u origin main\n```\n\n### Problem: Keinen Fehler, aber nichts passiert\n\nLösung: Git verbose machen, um zu sehen, was vorgeht\n```bash\nGIT_TRACE=1 GIT_CURL_VERBOSE=1 git push -u origin main\n```\n\n---\n\n## \u001b[34;1mÜBERPRÜFUNGSLISTE\001b[0m\n\n- [ ] Git ist installiert (`git --version`) > v2.35.0\n- [ ] Git Repository existiert (`ls -la .git`) > .git Ordner existiert\n- [ ] Git Commit existiert (`git log --oneline`) > Mindestens 1 Commit\n- [ ] GitHub Remote ist gesetzt (`git remote -v`) > origin zeigt zu GitHub\n- [ ] Code wurde pushed (`git push -u origin main`) > Kein Fehler\n- [ ] GitHub Site zeigt Code (`https://github.com/...`) > Alle Files sichtbar\n- [ ] Datenbank Backup erstellt (`ls -la backups/`) > Mindestens 1 Backup existiert\n\n---\n\n## \u001b[32;1mNÄCHSTE SCHRITTE\001b[0m\n\n1. Starte den Dev Server: `npm run dev`\n2. Teste die Friends feature:\n   - User A sendet Anfrage an User B\n   - User B akzeptiert\n   - Beide sehen sich in Freundesliste\n3. Überprüfe, ob alles in GitHub sichtbar ist (Backup erfolgreich)\n4. Erstelle regelmäßige Backups des DB Files\n5. Dokumentiere Änderungen in Commits (Feature-based).\n\n---\n\n**Viel Erfolg mit dem Setup!**\n\""