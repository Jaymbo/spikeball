"---\ntitle: Code Review Security Upgrades\ntags: [security, bcrypt, jwt, auth, code-quality]\n---\n# PROBLEM\nCode review revealed critical security vulnerabilities and code quality issues:\n1. **CRITICAL**: Hardcoded password salt ('spikeball-salt') in all password hashes - makes all passwords vulnerable to rainbow table attacks\n2. **CRITICAL**: JWT_SECRET has insecure fallback value - allows token forgery in production\n3. **CRITICAL**: Password verification was synchronous with crypto - possible timing attack for username enumeration\n4. **Code Quality**: Massive DRY violations in games/route.ts (4x repeated player update logic)\n5. **Code Quality**: Inefficient replay logic in DELETE /api/games\n6. **Security**: Password minimum length only 6 characters (insufficient)\n7. **Security**: No request validation in API endpoints (pagination DoS risk)\n\n# LÖSUNG\n1. Migrate from custom PBKDF2 to bcrypt password hashing\n2. Remove JWT_SECRET fallback - fail-fast on missing ENV\n3. Add timing-safe password compare for login failures\n4. Refactor game logic into reusable helper functions\n5. Add pagination limits to prevent performance issues\n6. Increase password minimum to 8 characters\n7. Add bcryptjs dependency and type definitions\n\n# CODE / COMMANDS\n\n## Package Installation\n```bash\nnpm install bcryptjs @types/bcryptjs --save\n```\n\n## Files Modified\n\n### src/lib/auth.ts\n- Replace crypto-based password functions with bcrypt\n- Add fail-fast JWT_SECRET validation on import\n- Make hash() and verifyPassword() async
\n- Add Salt: bcrypt generates unique salt per password automatically\n\n### src/app/api/auth/login/route.ts\n- Add timing-safe compare for missing users (prevents username enumeration)\n- Update password verification to async\n\n### src/app/api/auth/register/route.ts\n- Update password hashing to async\n- Change password min length from 6 to 8\n\n### src/app/api/auth/change-password/route.ts\n- Update password hashing and verification to async\n- Change password min length from 6 to 8\n\n### src/app/api/games/route.ts\n- Add import for game-utils helper functions\n- Add pagination validation (limit 1-100, offset >= 0)\n- Replace 4x repeated player update code with `updatePlayerStats()` helper\n- Replace EloChange.create() calls with single `createMany()` operation\n- Replace massive DELETE replay loop with `replayGame()` helper\n- Fix duplicate `new Date()` calls (use shared `gameDate` variable)\n\n### src/lib/game-utils.ts (NEW FILE)\n- Extract reusable game logic functions:\n  - `updatePlayerStats()` - Update player ELO and stats\n  - `createELOChangeRecords()` - Batch create ELO change records\n  - `replayGame()` - Replay a single game (for DELETE recalculation)\n  - `updatePlayerStatsInTx()` - Helper for transaction updates\n\n### env.example (NEW FILE)\n- Document required environment variables\n- Provide instructions for secure JWT_SECRET generation\n\n# MIGRATION STEPS (IMPORTANT!)\n\n## Password Migration\n⚠️ **CRITICAL**: bcrypt password hashes CANNOT be converted from the old crypto-based hashes!\n\nAll existing users must reset their passwords:\n\n1. Before deployment, mark all users as requiring password change:\n```sql\nUPDATE User SET requiresPasswordChange = 1;\n```\n\n2. Users will see a password change dialog on next login\n3. The change-password API now uses bcrypt for the new password\n\n## JWT Secret Setup\n1. Generate a secure JWT_SECRET (min 32 characters):\n```bash\nopenssl rand -base64 32\n```\n\n2. Add to `.env.local` (DO NOT commit to git!):\n```bash\nJWT_SECRET=\"<your-generated-secret>\"\n```\n\n3. Application will crash to prevent insecure startup if JWT_SECRET is missing\n\n# SECURITY IMPROVEMENTS\n- ✅ Industry-standard bcrypt password hashing (cost factor 12)\n- ✅ Unique salt per password (prevents rainbow table attacks)\n- ✅ Timing-safe login failures (prevents username enumeration)\n- ✅ Fail-fast JWT verification (no insecure defaults)\n- ✅ Stronger password requirements (min 8 chars)\n- ✅ API pagination limits (prevents DoS)\n\n# CODE QUALITY IMPROVEMENTS\n- ✅ Eliminated 4x code duplication in game player updates\n- ✅ Reduced DELETE replay logic from 60+ lines to 2 lines\n- ✅ Better separation of concerns (game-utils module)\n- ✅ Consistent async/await patterns\n- ✅ Removed duplicate date calculations\n\n# TESTED COMMANDS\n- `npm install bcryptjs @types/bcryptjs --save` - Successfully added\n- `git diff src/app/api/games/route.ts` - Line count reduced from ~240 to ~180\n\n# WEITERE RESOURCESC\n- bcryptjs: https://www.npmjs.com/package/bcryptjs\n- JWT Best Practices: https://datatracker.ietf.org/doc/html/rfc8725\n- OWASP Password Storage Cheat Sheet: https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html\n---"